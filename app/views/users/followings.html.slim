div.row.justify-content-center
  - if @user.followings.any?
    = content_tag(:h1, "#{@user.nickname}'s followings")
  - else  
    = content_tag(:h1, "#{@user.nickname}'s followings")
    div.w-100
    = content_tag(:h1, "#{@user.nickname} does not have any followings :(", style: 'margin-top: 1rem')
  div.w-100
  div.col-9
    - if @user.followings.any?
      == pagy_bootstrap_nav(@pagy)
      - @users.each do |user|
        = render user
    - if @user.followings.any?
      == pagy_bootstrap_nav(@pagy)          

